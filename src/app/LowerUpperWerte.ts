
/**
 * Interface für die beiden Werte eines Intervalls, die
 * mit einem Range (Slider) mit "dual knobs" dargestellt
 * oder gesetzt werden.
 */
export interface LowerUpperWerte {

  lower: number;
  upper: number;
}